pub struct LensProfileDatabase {
    pub identifier: String,
    pub calib_width: usize,
    pub calib_height: usize,
}

impl LensProfileDatabase {
    pub fn load_all(&mut self) {

    }

    pub fn get_by_id(&self, _id: &str) {

    }
}
